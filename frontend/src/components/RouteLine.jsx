// RouteLine.jsx

// import { useMap } from "react-leaflet"; ❌ Not needed now
// import L from "leaflet";
// import "leaflet-routing-machine"; ❌ Disabled to prevent slowdowns

const RouteLine = () => {
  // const map = useMap(); ❌ Not used anymore

  // 💤 Routing logic disabled temporarily to avoid performance issues
  return null;
};

export default RouteLine;
